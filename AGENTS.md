# 🤖 AI Agents & Skills (제안)

현재 Notepad 프로젝트(FE/BE)의 기능들을 확장하고 사용자 경험을 극대화하기 위해 추가할 수 있는 AI 에이전트와 스킬 아이디어 목록입니다.

## 1. 📝 Note Assistant Agent (노트 관리 보조)
사용자가 작성하는 노트를 분석하여 지식 관리를 자동화하는 에이전트입니다.
- **자동 요약 스킬 (Auto-Summarization)**: 내용이 긴 노트의 핵심을 파악해 상단에 3~4줄로 자동 요약해줍니다.
- **스마트 태깅 스킬 (Smart Tagging)**: 본문의 키워드와 문맥을 분석하여 적절한 태그를 자동 추천 및 부착합니다.
- **문맥 기반 검색 (Semantic Search)**: 단순 키워드 매칭이 아닌, 의미 기반으로 과거 노트를 검색할 수 있도록 지원합니다.

## 2. 🎰 Smart Lotto Analyzer (스마트 로또 분석기)
현재 단순 랜덤 생성인 로또 번호 생성 로직을 데이터 기반으로 고도화합니다.
- **과거 데이터 통계 스킬**: 역대 당첨 번호의 출현 빈도, 연속 번호 패턴, 미출현 번호 등을 분석합니다.
- **패턴 기반 추천 스킬**: 분석된 통계를 기반으로 사용자가 원하는 패턴(예: 홀짝 비율 맞춤, 최근 미출현 번호 포함 등)에 맞게 번호를 생성해줍니다.

## 3. 💒 Guestbook Sentiment Analyzer (방명록 감정 분석기)
결혼식 방명록에 작성된 메시지들을 분석하여 특별한 이벤트를 더욱 풍성하게 만듭니다.
- **메시지 감정 분석 스킬**: 텍스트에 담긴 감정(기쁨, 축하, 응원 등)을 분석하고 수치화합니다.
- **워드 클라우드 생성 스킬**: 하객들이 남긴 메시지에서 가장 많이 사용된 긍정적인 단어들을 추출해 대시보드에 워드 클라우드로 시각화해줍니다.

## 4. 🕵️ Security & Audit Agent (보안 및 감사 모니터링)
관리자가 시스템의 보안 상태를 쉽게 파악할 수 있도록 돕는 에이전트입니다.
- **비정상 행위 탐지 스킬 (Anomaly Detection)**: Audit Log(감사 로그)를 실시간 모니터링하여 평소와 다른 패턴(예: 짧은 시간 내 다수의 로그인 실패, 새벽 시간대의 대량 데이터 삭제 등)을 탐지합니다.
- **실시간 경고 스킬**: 비정상 행위 탐지 시, 관리자 대시보드에 즉각적인 알림이나 리포트를 생성해줍니다.

## 5. 💬 Personal Q&A Chatbot (개인 데이터 기반 챗봇)
Notepad에 저장된 사용자의 다양한 데이터(노트, 시리얼 번호, 계좌번호 등)에 자연어로 접근할 수 있게 합니다.
- **RAG (Retrieval-Augmented Generation) 스킬**: "최근에 작성한 React 관련 노트 찾아줘", "내 국민은행 계좌번호가 뭐지?"와 같은 자연어 질문을 이해하고 정확한 답변을 제공합니다.
- **보안 필터링 스킬**: 개인 정보나 민감한 데이터(비밀번호, 시리얼 번호)를 다루므로, 본인 인증이 완료된 상태에서만 답변하도록 철저한 권한 검증을 수행합니다.

---

### 🛠 구현 고려사항 (Implementation Notes)
- **프론트엔드 연동**: 위 에이전트들의 결과를 시각적으로 잘 보여주기 위해 Ant Design의 컴포넌트(예: Chat UI, Statistic, WordCloud 차트 등)를 활용하여 대시보드나 각 모듈 페이지에 통합할 수 있습니다.
- **백엔드 연동**: LLM(대형 언어 모델) API(예: OpenAI, Gemini 등)를 백엔드에서 호출하고 그 결과를 프론트엔드에 REST API로 제공하는 구조로 확장이 필요합니다.
